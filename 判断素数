#include<stdio.h>
#include<stdlib.h>

int main()

{
	int n = 0;
	int i = 1;

	scanf("%d",&n);

	for(i=2; i<n; i++)
	{
		if(n % i == 0)
		{
			printf("%d不是素数\n", n);
			break;
		}
	}

	if(i == n)
		printf("%d是素数\n",n);

	system("pause");
	return 0;
}
